# ASL Data Augmentation Guidelines

Transformation parameters to prevent overfitting:
- Random rotation: $\pm 15^\circ$.
- Random brightness adjustment: $\pm 10\%$.
- Rescale: $224 \times 224$ pixels.
